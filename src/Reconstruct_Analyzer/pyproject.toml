[project]
name = "kolam"
version = "1.0.0"
description = "Kolam fragment reconstruction and analysis studio"
requires-python = ">=3.12,<3.13"
dependencies = [
  "Flask>=3.0,<4.0",
  "Pillow>=10.0,<12.0",
  "numpy>=1.26,<3.0",
]

[tool.vercel]
entrypoint = "app:app"
